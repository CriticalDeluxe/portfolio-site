






---
import BaseLayout from "../layouts/BaseLayout.astro";
import { skills } from "../data/skills";
---



<BaseLayout title="Portfolio Home">
  <main>
  <section class="hero">
  <div class="hero-text">
    <h1>Hi, I’m Hudson.</h1>
    <p>
      I design and produce small physical and digital objects — from 3D-printed
      miniatures to game art, tooling, and experiments in form and process.
    </p>
    <p>
      This site is a living portfolio: each section breaks down how I work,
      what I’ve made, and how ideas move from concept to finished object.
    </p>
  </div>
  <figure class="hero-photo">
    <img
      src="/images/me.jpg"
      alt="Portrait of Hudson"
      width="420"
      height="420"
      loading="eager"
    />
  </figure>

</section>

  <section>
  <h2>Skills & Interests</h2>

  <div class="skills-grid">
    {skills.map((s) => (
      <a class="skill-card" href={`/skills/${s.slug}`}>
  <img class="skill-icon" src={s.icon} alt="" width="32" height="32" loading="lazy" />
  <div class="skill-text">
    <span class="skill-title">{s.title}</span>
    {s.blurb && <span class="skill-blurb">{s.blurb}</span>}
    
  </div>
</a>
    ))}
  </div>
</section>
</main>
</BaseLayout>
<style>

  main {
    max-width: 720px;
    margin: 2rem auto;
    padding: 0 1rem;
  }

  section {
    margin-bottom: 2.5rem;
  }

  .skills-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(210px, 1fr));
  gap: 1rem;
}

.skill-card {
  display: flex;
  align-items: center;
  gap: .75rem;
  padding: 1rem;
  border: 1px solid #ccc;
  border-radius: 8px;
  text-decoration: none;
}

.skill-card:hover {
  background: #000000;
}

.skill-icon {
  flex: 0 0 auto;
  width: 64px;
  height: 64px;
  object-fit: contain;
}

  .skills-grid a:hover {
    background: #f5f5f5;
  }
  .hero {
  display: grid;
  grid-template-columns: 1.2fr 0.8fr;
  gap: 1.5rem;
  align-items: start;
}
.skill-text { display: grid; gap: .15rem; }
.skill-title { font-weight: 600; }
.skill-blurb { font-size: .9rem; opacity: .85; }

.hero-photo {
  margin: 0;
}

.hero-photo img {
  width: 100%;
  height: auto;
  border-radius: 10px;
  border: 1px solid #ccc;
}

@media (max-width: 760px) {
  .hero {
    grid-template-columns: 1fr;
  }
}
</style>
